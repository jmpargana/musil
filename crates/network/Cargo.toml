[package]
name = "network"
version = "0.1.0"
edition = "2024"

[dependencies]
broker = { path = "../broker" }
storage = { path = "../storage" }
bytes = "1"
tokio = { version = "1", features = ["full"] }
tracing = "0.1"

[dev-dependencies]
tempdir = "0.3"
